{
  "name": "freelang-v4-crypto",
  "version": "1.0.0",
  "description": "FreeLang v4 Crypto Functions — 5개 내장 함수 (hash, hmac, encrypt, decrypt, uuid) — Hardened Security Runtime",
  "scripts": {
    "start": "npx ts-node src/main.ts",
    "test": "npx ts-node tests/crypto.test.ts"
  },
  "devDependencies": {
    "typescript": "^5.0.0",
    "ts-node": "^10.0.0",
    "@types/node": "^20.0.0"
  }
}
